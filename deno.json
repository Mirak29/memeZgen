{
  "compilerOptions": {
    "jsx": "react-jsx",
    "jsxImportSource": "preact"
  },
  "fmt": {
    "useTabs": false,
    "lineWidth": 80,
    "indentWidth": 2,
    "semiColons": false,
    "singleQuote": true
  },
  "lint": {
    "rules": {
      "tags": ["recommended"],
      "exclude": ["no-unversioned-import"]
    }
  },
  "tasks": {
    "lint": "deno lint",
    "check": "deno check web api tasks",
    "fmt": "deno fmt",
    "fmt:check": "deno fmt --check",
    "vite": "deno run -A tasks/vite.ts",
    "dev:back": "deno serve --port 3001 --allow-env --allow-net api/server.ts",
    "dev": "deno run --allow-all dev.ts",
    "build": "deno run -A npm:vite build",
    "preview": "deno run --allow-env --allow-sys --allow-read --allow-write --allow-net --allow-run --allow-ffi npm:vite preview"
  }
}
